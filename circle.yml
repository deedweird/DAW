deployment:
    dockerhub:
        branch: master
        commands:
            - curl -X POST -H "Content-Type: application/json" --data '{"build": true}' $DOCKER_HUB_TRIGGER_URL

test:
    override:
        - mocha

